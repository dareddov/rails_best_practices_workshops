= simple_form_for @offer, :html => { :class => "form-horizontal" } do |f|
  -if @offer.errors.any?
    .alert.alert-danger.alert-dismissable
      %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} &times;
      %h4= "#{pluralize(@offer.errors.count, "error")} prohibited this offer from being saved:"

      %ul
        - @offer.errors.full_messages.each do |msg|
          %li= msg



  = f.label :owner_id, class => 'col-sm-2 control-label'
  = f.label :message, class => 'col-sm-2 control-label'
  = f.button :submit, class => 'col-sm-2 control-label'